-- Histórico: a tabela `whatsapp_templates` é usada pelo backend (whatsapp-test.js, whatsapp-service.js).
-- Em ambientes novos ela costuma ser criada via Supabase / schema principal.
-- Seeds adicionais: 20260404_seed_whatsapp_templates_ponto.sql
SELECT 1;
